using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CloseMiddleUI : MonoBehaviour
{
    /*private bool x = false;
    static float t = 0f;
    public void Update()
    {
        if (x == true)
        {
            rt.offsetMin = new Vector2(rt.offsetMin.x, Mathf.Lerp(0f, -473f, t));
            t += 2f * Time.deltaTime;
            if (t > 1.0f)
            {
                x = false;
            }
        }
    }*/
}
